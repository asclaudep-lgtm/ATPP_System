# GUI
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0
# Fluent Design widgets (Win11-style buttons, icons, dialogs).
# Используется как drop-in замена стандартных QPushButton/QLineEdit и т.д.
# (см. ui/fluent_compat.py, ui/theme.py).
# Если пакет недоступен на target-машине, ui/fluent_compat.py
# падает в graceful fallback на стандартные PyQt6-виджеты — приложение работает.
PyQt6-Fluent-Widgets>=1.6.0
# Резервная база тёмной/светлой темы (если qfluentwidgets недоступен).
pyqtdarktheme>=2.1.0

# Database
SQLAlchemy>=2.0.0
alembic>=1.13.0

# Documents
openpyxl>=3.1.0
python-docx>=1.1.0
reportlab>=4.0.0

# Utils
python-dateutil>=2.8.0
bcrypt>=4.1.0

# Optional
psycopg2-binary>=2.9.0  # PostgreSQL support
matplotlib>=3.8.0        # Charts
python-barcode>=0.15.1   # Code128 для модуля "Производство"
Pillow>=10.0.0           # для PNG-вывода штрих-кодов

# v10: Web client
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-jose[cryptography]>=3.3.0
pydantic>=2.0.0

# v10: IoT
paho-mqtt>=2.1.0

# v10: AI Assistant, CAD
numpy>=1.26.0
steputils>=0.1

# v10: Testing
httpx>=0.27.0
