# ATPP_System v8 — «Быстрые улучшения»

> Релиз, в котором сделаны **все 10 пунктов** из блока «🟢 Быстрые улучшения»
> v8-roadmap. Версионная семантика: v8 не ломает данные v7 — все миграции
> ALTER TABLE … ADD COLUMN накатываются автоматически при первом запуске
> через `DatabaseManager.init_database()`.

---

## Что нового по пунктам

### 1. Глобальный поиск Ctrl+P  (`v8-1`)
- Хоткей **Ctrl+P** в любой вкладке открывает модалку быстрого поиска.
- Старый постоянный поиск (`Ctrl+Shift+F`) остался — это полная вкладка с
  фильтрами.
- Ищет: детали, ТП, операции, переходы, материалы, оборудование, людей.
- Двойной клик / Enter — открывает ТП и закрывает модалку.

Файлы:
- `ui/dialogs/global_search_dialog.py` (новый)
- `ui/main_window.py` — регистрация хоткея.

### 2. Корзина для ТП / Операций / Деталей / Нарядов  (`v8-2`)
- В v7 корзина была только для ТП и операций. v8 расширил её на
  **Изделия** и **Наряды**.
- Soft-delete: запись помечается `is_deleted=True`, `deleted_at`,
  `deleted_by`. Восстановление — кнопкой «↺ Восстановить».
- **Автоочистка через 30 дней** — отдельная кнопка «🗑 Очистить
  старше 30 дней» + автоматическая чистка при открытии корзины.
- Хоткей **Ctrl+B** — открыть корзину.
- Тесты: `test_v8_features.py::test_product_soft_delete_persists`,
  `test_workorder_soft_delete_persists`, `test_recycle_bin_filters_live_products`.

Файлы:
- `database/models.py` — поля `is_deleted/deleted_at/deleted_by` у Product, WorkOrder.
- `database/db_manager.py` — миграция новых колонок.
- `ui/widgets/recycle_bin.py` — 4 вкладки.

### 3. Тёмная тема + крупный шрифт  (`v8-3`)
- Кнопка-переключатель **☀ / 🌙** в правом верхнем углу toolbar.
- Кнопка-переключатель **«А+ / А−»** для размера шрифта.
- Сохраняется в `settings` (как и было в v7).

Файлы:
- `ui/main_window.py` — кнопки в toolbar.

### 4. Печать пачкой МТП  (`v8-4`)
- В Производстве — таблица нарядов теперь **multi-select**.
- Новая кнопка **«📦 Пачка МТП…»** — выделить N нарядов и:
  - либо собрать **один многостраничный PDF** (page-break между ТП);
  - либо сохранить **N отдельных xlsx** в выбранную папку.
- Старая кнопка «📄 МТП (xlsx)» для одного наряда — сохранена.

Файлы:
- `ui/widgets/production_widget.py` — `_on_mtp_batch()`.

### 5. Шаблоны переходов  (`v8-5`)
- Новая таблица `transition_templates` (модель `TransitionTemplate`).
- Стартовый набор из **18 типовых формулировок** (точение, фрезерование,
  слесарная, ОТК и т.д.). Загружается при первом открытии библиотеки.
- Меню **Сервис → Шаблоны переходов** — полный CRUD.
- В диалоге перехода — кнопка **«📋 Из шаблона…»**: открывается та же
  библиотека в режиме `pick_mode`, двойной клик подставляет текст.

Файлы:
- `ui/dialogs/transition_templates_dialog.py` (новый, ~280 строк).
- `ui/dialogs/transition_dialog.py` — кнопка «Из шаблона…».

### 6. Себестоимость по факту  (`v8-6`)
- В виджете расчёта себестоимости — комбобокс **«Время операций»**:
  - **«по плановой T-шт.»** (default, как было).
  - **«по факту (из журналов)»** — берёт среднее время по
    `RouteStep.finished_at - started_at`.
- Если факт.шагов нет — fallback на план.
- Режим **«по факту» НЕ сохраняется** в БД (это разовый снимок «как
  было»), чтобы не затирать плановый расчёт.

Файлы:
- `modules/cost_calc.py` — `TIME_MODE_PLAN/ACTUAL`,
  `_avg_actual_minutes()`.
- `ui/widgets/cost_calc_widget.py` — комбобокс + флаг `persist`.

Тесты:
- `test_cost_calc_uses_actual_time_when_route_steps_exist`
- `test_cost_calc_actual_fallback_to_plan_when_no_routesteps`

### 7. Экспорт МТП в PDF  (`v8-7`)
- Новый модуль `modules/mtp_pdf.py` (reportlab + Platypus).
- Структура страницы совпадает с xlsx-вариантом из v7: шапка, блок ДСЕ,
  таблица материалов, таблица операций, нижний колонтитул.
- Поддержка кириллицы — DejaVuSans/DejaVuSans-Bold из
  `resources/fonts/`.
- Кнопка **«📄 МТП (PDF)»** на вкладке Производство (рядом с xlsx).
- Тоже работает с режимом «свернуть промежуточные» / «полный».
- Используется в «Пачке МТП» (см. #4).

Тесты:
- `test_generate_mtp_pdf_single` — single tech_process_id.
- `test_generate_mtp_pdf_batch` — N ТП в один документ.

### 8. Авто-номер операции с шагом 5  (`v8-8`) — *было в v7*
- Уже работало (`modules/settings.next_op_number`). Подтверждено.

### 9. Bulk-замена оборудования  (`v8-9`) — *было в v7*
- `BulkEditOpsDialog` через кнопку «⚙ Массовое изменение» в редакторе
  ТП. Уже работало в v7, подтверждено.

### 10. Индикатор «недозаполненный ТП»  (`v8-10`)
- Новый модуль `modules/completeness.py` — формула оценки полноты
  заполнения ТП (материал, нормы, оборудование, грейды, время).
- В дереве групп — эмодзи-индикатор перед номером ТП:
  - 🟢 ≥ 80 % (готов к производству).
  - 🟡 50–79 % (есть пропуски).
  - 🔴 < 50 % (черновик).
- В заголовке КТП — компактная плашка «Полнота: 73 % (8/11 пунктов)».

Файлы:
- `modules/completeness.py` (новый).
- `ui/widgets/group_tree.py` — отображение индикатора.
- `ui/widgets/product_ktp.py` — плашка.
- `ui/widgets/data_completeness.py` — старый дашборд, тоже использует
  этот модуль.

Тесты:
- `test_completeness_module_score_tp_empty`
- `test_completeness_module_score_tp_full`

---

## Совместимость

- **БД v7 → v8:** автоматическая миграция при первом запуске
  (`DatabaseManager.init_database()`). Новые колонки добавляются через
  ALTER TABLE, существующие данные не трогаются.
- **БД v8 → v7:** откат не поддерживается (новые колонки не помешают v7,
  но новые таблицы `transition_templates` будут проигнорированы).

## Тесты

```
102 passed, 182 warnings in 42.56s
```

Из них **11 новых v8-тестов** в `tests/test_v8_features.py`:
покрывают soft-delete, шаблоны переходов, расчёт по факту,
генерацию PDF, метрику полноты.

## Скриншоты

См. вложения к этому релизу:

| Файл | Что показывает |
|---|---|
| `01_recycle_bin.png` | Корзина с 4 вкладками |
| `02_transition_templates.png` | Библиотека шаблонов переходов |
| `03_cost_calc_actual.png` | Расчёт себестоимости — режим план/факт |
| `04_global_search_ctrl_p.png` | Глобальный поиск Ctrl+P |
| `05_production_with_mtp_pdf_batch.png` | Производство: кнопки «МТП (PDF)» и «Пачка МТП…» |
| `06_mtp_pdf_preview.png` | Превью сгенерированного PDF МТП |

## Архивы

- `ATPP_System_v8_linux.zip` — исходники + БД 211 деталей + готовый Linux .venv.
- `ATPP_System_v8_windows.zip` — исходники + БД + 30 wheel-файлов под Windows x64 / Python 3.12 + `install_windows.bat`.
- `ATPP_System_v8_src.zip` — только исходники без БД и venv (~5 МБ).
